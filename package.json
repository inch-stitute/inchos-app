{
  "name": "inchos-app",
  "module": "index.ts",
  "type": "module",
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "lint": "bunx @biomejs/biome check --apply ."
  },
  "dependencies": {
    "hono": "^3.12.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.5.0",
    "@types/bun": "^1.0.3"
  }
}
