{
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "lint": "bunx @biomejs/biome check --apply ."
  },
  "dependencies": {
    "hono": "^3.12.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.5.0",
    "@types/bun": "^1.0.0"
  }
}
